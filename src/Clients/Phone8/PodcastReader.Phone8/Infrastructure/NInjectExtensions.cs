using System.Collections.Generic;
using System.Linq;
using Ninject;

namespace PodcastReader.Phone8.Infrastructure
{
    public static class NInjectExtensions
    {
        //public static T GetLastRegistered<T>(this IKernel This, string contract = null)
        //{
        //    var binding = This
        //        .GetBindings(typeof (T))
        //        .LastOrDefault(b => contract == null || b.Metadata.Name == contract);

        //    if (binding != null)
        //        This.
        //}
    }
}