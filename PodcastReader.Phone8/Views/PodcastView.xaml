<phone:PhoneApplicationPage
    x:Class="PodcastReader.Phone8.Views.PodcastView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:m="clr-namespace:Magellan.WP.Controls;assembly=Magellan.WP"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <m:Layout Source="/PodcastReader.Phone8;component/Views/Layouts/HeaderedLayout.xaml">

        <m:Zone ZonePlaceHolderName="Header" Content="{Binding Title}" />
        
        <m:Zone ZonePlaceHolderName="Content">
            <StackPanel Background="Transparent">
                <ScrollViewer>
                    <ItemsControl ItemsSource="{Binding Items}" />
                </ScrollViewer>
            </StackPanel>
        </m:Zone>
        
    </m:Layout>

</phone:PhoneApplicationPage>